---
- hosts: all
  vars_prompt:
    - name: usuario
      prompt: Usuario del curso a borrar 
      private: no
  
  tasks:

    - name: delete users
      win_user:
        name: "{{ usuario }}"
        state: absent